#include <iostream>
using namespace std;

int main() {
    int a, b, c, d, e;
    cout << "Nhap so thu nhat: "; cin >> a;
    cout << "Nhap so thu hai: "; cin >> b;
    cout << "Nhap so thu ba: "; cin >> c;
    cout << "Nhap so thu tu: "; cin >> d;
    a = e;

    if (b < e) {
        e = b;
    }
    if (c < e) {
        e = c;
    }
    if (d < e) {
        e = d;
    }
    cout << "So nho nhat la: " << e;

    return 0;
}
    